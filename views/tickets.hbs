{{!-- Γεια σου Νίκο!
    Backend provides:
    - outboundFlights: Always an array (empty if no outbound flights).
    - returnFlights: Always an array (empty for one-way trips).
--}}
<div class="page-container">
    <h2>Available Flights</h2>

    {{#if tickets.length}}
        <ul class="tickets-list">
            {{#each tickets}}
                <li class="ticket-card">
                    <strong>{{departure_city}}</strong> → <strong>{{arrival_city}}</strong><br/>
                    Airline: {{airline_name}}<br/>
                    Class: {{class}}<br/>
                    Departure: {{time_departure}} | Arrival: {{time_arrival}}<br/>
                    Price: ${{price}}
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No flights found. Try searching again!</p>
    {{/if}}
</div>
