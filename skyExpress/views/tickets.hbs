<div class="page-container">
    <h2>Available Flights</h2>

    {{#if tickets.length}}
        <ul class="tickets-list">
            {{#each tickets}}
                <li class="ticket-card">
                    <strong>{{from}}</strong> → <strong>{{to}}</strong><br/>
                    Class: {{flightClass}} | Trip: {{tripType}}<br/>
                    Adults: {{adults}} | Children: {{children}}<br/>
                    Price: ${{price}}
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>No flights found. Try searching again!</p>
    {{/if}}
</div>
